package main

import "fmt"

func main()  {
	var number =2
	name := "codingbitz" // Walrus Operator
	var id uint32 = 12345

	/// Default Values
	var idz uint16
	var bl bool
	var er string
	
	//fmt.Printf("Hello %T %v",name,id)
//	var x string = fmt.Sprintf("Hello %T %v",name,id) // Saves Print statement into variables.
//	fmt.Println(x)
	
	fmt.Println(name, id, number)
	fmt.Println(idz,bl,er)

	fmt.Printf("Number: %q","cbitz")
}