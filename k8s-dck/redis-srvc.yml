apiVersion: v1
kind: Service
metadata:
  name: redis
  labels:
    name: redis-srvc
    app: demovoting-app
spec:
  selector:
    name: redis-pod
    app: demovoting-app
  ports:
  - port: 6379
    targetPort: 6379
